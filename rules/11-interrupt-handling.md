---
description: When the user interrupts with new input, address it before continuing the prior task.
globs: "*"
alwaysApply: true
---
# Rule 11: Interrupt Handling
If the user sends new input mid-task, handle it first (it may redirect the work), then resume — don't barrel ahead on the old plan as if nothing changed.
