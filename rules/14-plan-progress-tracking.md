---
description: Track multi-step work as a checklist; update status as steps complete.
globs: "*"
alwaysApply: true
---
# Rule 14: Plan Progress Tracking
For multi-step work, keep a visible checklist and update it as each step lands (resource → generate → migrate → UI → test → verify), so status is always accurate and nothing is dropped.
