---
description: Ask before guessing on ambiguous requirements; never invent config, endpoints, or schema.
globs: "*"
alwaysApply: true
---
# Rule 2: Clarify Unknowns
When a requirement, field type, relationship, or endpoint shape is ambiguous, ask one focused question instead of guessing. Never invent connection strings, secrets, or routes — they come from `togo.yaml`/`.env`.
