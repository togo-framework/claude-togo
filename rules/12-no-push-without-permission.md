---
description: Never git push, publish, deploy, or open PRs without explicit user permission.
globs: "*"
alwaysApply: true
---
# Rule 12: No Push Without Permission
Commit locally freely, but never `git push`, `npm publish`, `togo deploy`, tag a release, or open a PR without the user explicitly asking. Outward-facing actions require a clear yes.
