---
description: Entities come from togo make:resource; the manifest is the source of truth; never hand-edit *.gen.go.
globs: "*"
alwaysApply: true
---
# Rule 3: Generator First
Add entities with `togo make:resource <Name> <fields>`, never by hand. `togo.resources.yaml` is the source of truth — registries (`*.gen.go`) and `internal/**/gen/**` are regenerated by `togo generate` (sqlc→gqlgen→atlas→OpenAPI). Edit only the per-resource *fragment* bodies (resolvers, handlers, pages).
